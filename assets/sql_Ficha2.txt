SELECT empnum, nome, chefe 
FROM EMP

SELECT salario/12 
FROM EMP

SELECT nome, salario + coalesce(comissao, 0)
FROM EMP

SELECT DISTINCT deptnum
FROM EMP

SELECT nome, funcao, salario
FROM EMP
ORDER BY nome 

SELECT empnum, nome, funcao, deptnum
FROM EMP
WHERE funcao='Admin'

SELECT nome, salario
FROM EMP
WHERE salario >= 20000

SELECT nome, salario
FROM EMP
WHERE salario BETWEEN 15000 and 20000

SELECT nome, chefe
FROM EMP
WHERE chefe in (33, 39)

SELECT nome
FROM EMP
WHERE nome like 's%'

SELECT nome, funcao, salario
FROM EMP
WHERE salario >= 16000 and funcao = 'Vendedor'

SELECT nome, chefe
FROM EMP
WHERE chefe is null

SELECT empnum, nome, salario + coalesce(comissao,0), data_adm, funcao, deptnum
FROM EMP
WHERE salario + coalesce(comissao, 0) >= 17000 and funcao = 'Vendedor' and deptnum = 10 and data_adm < '2000.july.01'

14.SELECT empnum, nome
FROM EMP
WHERE comissao is null

15.SELECT nome, salario, salario * 1.1
FROM EMP

SELECT nome, empnum, comissao, data_adm
FROM EMP
WHERE comissao is null
ORDER BY data_adm

SELECT nome
FROM EMP
WHERE nome like '_A%S'

SELECT TOP 1 nome, data_adm
FROM EMP
ORDER BY data_adm

SELECT nome, data_adm
FROM EMP
ORDER BY data_adm
